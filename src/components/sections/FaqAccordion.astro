---
import type { FaqItem } from '../../data/faq';

interface Props {
  items: FaqItem[];
  idPrefix?: string;
}
const { items, idPrefix = 'faq' } = Astro.props;
---

<div class="divide-y divide-brand-100/30 rounded-card bg-white border border-brand-100/40 shadow-card overflow-hidden">
  {items.map((item, i) => {
    const panelId = `${idPrefix}-panel-${i}`;
    const btnId = `${idPrefix}-btn-${i}`;
    return (
      <div class="faq-item">
        <h3>
          <button
            id={btnId}
            aria-expanded="false"
            aria-controls={panelId}
            class="faq-trigger w-full flex items-center justify-between gap-4 px-5 py-4 text-right text-base font-semibold text-brand hover:bg-canvas-100 transition-colors"
          >
            <span>{item.question}</span>
            <span class="faq-icon shrink-0 flex h-8 w-8 items-center justify-center rounded-full bg-brand-50 text-brand text-lg leading-none transition-transform duration-300" aria-hidden="true">+</span>
          </button>
        </h3>
        <div id={panelId} role="region" aria-labelledby={btnId} class="faq-panel hidden px-5 pb-4">
          <p class="text-sm leading-7 text-ink-soft">{item.answer}</p>
        </div>
      </div>
    );
  })}
</div>

<script>
  document.querySelectorAll('.faq-trigger').forEach((btn) => {
    btn.addEventListener('click', () => {
      const expanded = btn.getAttribute('aria-expanded') === 'true';
      const panel = document.getElementById(btn.getAttribute('aria-controls') || '');
      const icon = btn.querySelector('.faq-icon');
      btn.setAttribute('aria-expanded', String(!expanded));
      panel?.classList.toggle('hidden', expanded);
      if (icon) {
        icon.textContent = expanded ? '+' : '−';
        (icon as HTMLElement).style.transform = expanded ? 'rotate(0deg)' : 'rotate(180deg)';
      }
    });
  });
</script>
